## Textures

Optional texture files for enhanced visual effects.
Currently all textures are generated via CSS (SVG noise, gradients).

If you want to add custom textures:
- `paper-grain.png` — aged paper texture for the notebook panel
- `concrete.png` — wall texture for the interrogation chat background
- Use small tileable textures (256x256 or 512x512) for best performance.
